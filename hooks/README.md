# hooks

Hooks de Claude Code (configuración de `settings.json` en la sección
`hooks`, más los scripts que ejecutan). Todavía no hay ninguno
configurado — carpeta lista para cuando se defina el primero.
